# v0.5.2.a `/councildone` Artifact Synthesizer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship `/councildone [preset]` user command that synthesizes a TL;DR-bearing markdown artifact from the current segment's discussion, persists it to a new `council_artifacts` table, and exposes it via `/councilshow <id>`.

**Architecture:** New `ArtifactService` owns segment-seal + DB-insert + open-new-segment as a single-owner mutation (continues v0.5.2 P1-B's listener-mutation-antipattern lesson). New `artifact-synthesizer` worker role (excluded from peer pool, never invoked by `runDeliberation`). New cross-table segment-counter helper shared between `ArtifactService` and `SessionReset` so both sealing primitives stay monotonic across process restarts. Bidirectional `synthesisInFlight` lock owned by `DeliberationHandler`.

**Tech Stack:** TypeScript (Node ESM), `better-sqlite3` for the new artifacts table, vitest for tests, existing `LLMProvider.chat()` for synthesizer invocation (no `AbortSignal` threading; caller-side `Promise.race` timeout).

**Spec:** [`docs/superpowers/specs/2026-04-25-councildone-artifact-design.md`](../specs/2026-04-25-councildone-artifact-design.md) — 13 codex review rounds, 0 P1 ship gate.

---

## Pre-Flight: Spec Open Items

Before starting Task 1, two spec §19 open items are resolved here so they aren't re-litigated mid-implementation:

- **§19.1 cross-module counter** → Option A: new `src/council/segment-counter.ts` exports `computeNextSegmentIndex(threadId, resetDb, artifactDb, handler)`. Both `ArtifactService.synthesize` and `SessionReset.reset` import it.
- **§19.2 `effectiveRoleType` placement** → `src/types.ts`, next to `WorkerRoleType`.

Two spec §12 deviations from existing codebase conventions (intentional, NOT spec violations):

- **CLI commands NOT in `src/cli/commands/`** — that directory does not exist; existing 8 council commands all live as methods on `CliCommandHandler` in `src/adapters/cli-commands.ts`, dispatched from `src/adapters/cli-dispatch.ts`. We follow that pattern for `/councildone` and `/councilshow`.
- **Telegram commands NOT in `src/telegram/commands/`** — that directory does not exist; existing council commands wire through `wireXxx` methods on `TelegramAdapter` (`src/adapters/telegram.ts`), registered by `src/telegram/bot.ts`. We follow that pattern.

---

## File Structure

**New files (10):**

| File | Responsibility |
|---|---|
| `src/council/artifact-db.ts` | sqlite layer for `council_artifacts` table (mirrors `src/storage/reset-snapshot-db.ts` shape) |
| `src/council/artifact-prompt.ts` | `buildArtifactPrompt(preset, transcript, model)` — preset-specific systemPrompt + user message construction, NO `systemPromptParts` (skips personality.ts markdown ban) |
| `src/council/artifact-invoke.ts` | `invokeProviderForArtifact()` with caller-side `Promise.race` timeout + `clearTimeout` in `finally`; `ProviderTimeoutError` class |
| `src/council/artifact-errors.ts` | error classes: `MissingSynthesizerConfigError`, `ArtifactEmptySegmentError`, `SynthesisAlreadyRunningError`, `ArtifactResetInFlightError`, `ArtifactDeliberationInFlightError`, `PendingClassificationError`, `ArtifactBlindReviewActiveError`, `MalformedArtifactError`, `EmptyResponseError`, `ProviderTimeoutError`, `SynthesisRetryExhaustedError` |
| `src/council/artifact-service.ts` | `ArtifactService` class: `synthesize(threadId, preset)`, `fetchByThreadLocalSeq(threadId, seq)`, `lastSealedSegmentIndex(threadId)` |
| `src/council/segment-counter.ts` | `computeNextSegmentIndex(threadId, resetDb, artifactDb, handler)` — single source of truth for cross-table seal-counter formula |
| `src/util/chunk-markdown.ts` | `chunkMarkdown(text, maxChars)` shared chunking helper (4-step algorithm from spec §9) |
| `tests/council/artifact-db.test.ts` | DB layer tests |
| `tests/council/artifact-service.test.ts` | service unit tests |
| `tests/council/segment-counter.test.ts` | counter helper unit tests |
| `tests/council/artifact-prompt.test.ts` | prompt builder tests |
| `tests/council/artifact-invoke.test.ts` | invoke timeout helper tests |
| `tests/util/chunk-markdown.test.ts` | chunking algorithm tests |
| `tests/integration/council-done-flow.test.ts` | end-to-end happy paths |
| `tests/integration/council-done-guards.test.ts` | guard ordering, missing config, mutual exclusion |
| `docs/synthesizer-config.md` | user-facing doc on adding an `artifact-synthesizer` agent config |

**Modified files (12):**

| File | Change |
|---|---|
| `src/types.ts` | Export `WorkerRoleType = 'peer' \| 'facilitator' \| 'artifact-synthesizer'`; change `AgentConfig.roleType` to `WorkerRoleType \| undefined` (stays optional); export `effectiveRoleType(w: AgentConfig): WorkerRoleType` |
| `src/events/bus.ts` | Add `'artifact.created'` to `EventMap` with `{threadId, segmentIndex, threadLocalSeq, preset}` payload |
| `src/council/deliberation.ts` | Add private `synthesisInFlight: Set<number>`, public accessors; extend `sealCurrentSegment(t, snapshotId: string \| null)` to accept null; new `unsealCurrentSegment(t)`; `runDeliberation` rejects when `isSynthesisInFlight(t)` |
| `src/council/session-reset.ts` | Add `handler.isSynthesisInFlight(t)` guard with persistent message; replace `:172` segment-index lookup with `computeNextSegmentIndex(...)`; constructor accepts `ArtifactDB` |
| `src/worker/providers/custom.ts` | One-line: attach `.status` to thrown error |
| `src/adapters/cli-commands.ts` | Add `councildone`, `councilshow` to whitelist; new methods on `CliCommandHandler` |
| `src/adapters/cli-dispatch.ts` | Route the new commands to async handler |
| `src/adapters/telegram.ts` | New `wireCouncilDone` / `wireCouncilShow` methods |
| `src/telegram/bot.ts` | Register `bot.command(...)` BEFORE catch-all using `resolveTelegramThreadId(ctx.message)` |
| `src/index.ts` | `pickFirstPeerConfig` for IntentGate + listener fallback; `peerWorkers` filter via `effectiveRoleType`; instantiate `ArtifactDB` + `ArtifactService`; pass `ArtifactDB` to `SessionReset` |
| `CHANGELOG.md` | v0.5.2.a entry |
| `package.json` | Version bump 0.5.1 → 0.5.2 |

**Pre-task housekeeping (Task 0):** add `.codex-review*.md` to `.gitignore` so scratch review transcripts don't enter PR diffs.

---

## Task 0: Repo Hygiene + Branch Setup

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: Verify clean working tree, create branch**

Run:

    cd ~/Projects/agent-council
    git status --short

Expected: only the spec doc + plan + `.codex-review-*.md` files are visible. No staged or modified tracked files.

    git checkout main
    git pull --rebase origin main
    git checkout -b v0.5.2.a-councildone-artifact

- [ ] **Step 2: Add to `.gitignore`**

Append:

    # Local codex review transcripts (scratch, not for PR diffs)
    .codex-review*.md

Untrack any (none expected):

    git ls-files | grep '^\.codex-review' || echo "none tracked, OK"

- [ ] **Step 3: Commit hygiene + spec + plan**

    git add .gitignore docs/superpowers/specs/2026-04-25-councildone-artifact-design.md docs/superpowers/plans/2026-04-26-v0.5.2.a-councildone-artifact.md
    git commit -m "chore(v0.5.2.a): add spec, plan, and codex-review gitignore"

---

## Task 1: `WorkerRoleType` + `effectiveRoleType` in types.ts

**Files:**
- Modify: `src/types.ts`
- Test: `tests/types.test.ts`

- [ ] **Step 1: Write failing test**

Append to `tests/types.test.ts`:

    import { describe, it, expect } from 'vitest';
    import { effectiveRoleType, type AgentConfig, type WorkerRoleType } from '../src/types.js';

    describe('effectiveRoleType', () => {
      const baseConfig: AgentConfig = {
        id: 'a', name: 'A', provider: 'custom', model: 'm',
        memoryDir: '.', personality: '',
      };

      it('returns explicit roleType when set', () => {
        expect(effectiveRoleType({ ...baseConfig, roleType: 'facilitator' })).toBe('facilitator');
        expect(effectiveRoleType({ ...baseConfig, roleType: 'artifact-synthesizer' })).toBe('artifact-synthesizer');
        expect(effectiveRoleType({ ...baseConfig, roleType: 'peer' })).toBe('peer');
      });

      it('defaults undefined roleType to peer', () => {
        expect(effectiveRoleType(baseConfig)).toBe('peer');
      });
    });

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/types.test.ts -t "effectiveRoleType"`. Expected: FAIL.

- [ ] **Step 3: Add type alias + helper in src/types.ts**

In the existing `AgentConfig` interface area:

    export type WorkerRoleType = 'peer' | 'facilitator' | 'artifact-synthesizer';

    export interface AgentConfig {
      id: string;
      name: string;
      provider: string;
      model: string;
      memoryDir: string;
      personality: string;
      botTokenEnv?: string;
      topics?: string[];
      roleType?: WorkerRoleType;
      models?: { low: string; medium: string; high: string };
      defaultModelTier?: Complexity;
      thinking?: Partial<Record<Complexity,
        | { mode: 'adaptive' }
        | { mode: 'enabled'; budget_tokens: number }
      >>;
      cacheSystemPrompt?: boolean;
    }

    /**
     * Treat undefined roleType as 'peer' (v0.5.1 backwards-compat).
     * All call sites that branch on roleType MUST go through this helper.
     */
    export function effectiveRoleType(w: AgentConfig): WorkerRoleType {
      return w.roleType ?? 'peer';
    }

- [ ] **Step 4: Run test, type-check, commit**

    npx vitest run tests/types.test.ts -t "effectiveRoleType"
    npx tsc --noEmit
    git add src/types.ts tests/types.test.ts
    git commit -m "feat(v0.5.2.a): add WorkerRoleType + effectiveRoleType helper"

---

## Task 2: Extend EventMap with `artifact.created`

**Files:**
- Modify: `src/events/bus.ts`
- Test: `tests/events/bus-artifact-created.test.ts` (new)

- [ ] **Step 1: Write failing test**

Create `tests/events/bus-artifact-created.test.ts`:

    import { describe, it, expect } from 'vitest';
    import { CouncilEventBus } from '../../src/events/bus.js';

    describe('artifact.created event', () => {
      it('emits and receives a typed artifact.created payload', () => {
        const bus = new CouncilEventBus();
        let received: { threadId: number; segmentIndex: number; threadLocalSeq: number; preset: 'universal' | 'decision' } | null = null;
        bus.on('artifact.created', (e) => { received = e; });

        bus.emit('artifact.created', {
          threadId: 42, segmentIndex: 3, threadLocalSeq: 1, preset: 'universal',
        });

        expect(received).toEqual({ threadId: 42, segmentIndex: 3, threadLocalSeq: 1, preset: 'universal' });
      });
    });

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/events/bus-artifact-created.test.ts`. Expected: FAIL (TS compile error).

- [ ] **Step 3: Add event to EventMap**

In `src/events/bus.ts`, add to `EventMap`:

    'artifact.created': {
      threadId: number;
      segmentIndex: number;
      threadLocalSeq: number;
      preset: 'universal' | 'decision';
    };

- [ ] **Step 4: Run test, type-check, commit**

    npx vitest run tests/events/bus-artifact-created.test.ts
    npx tsc --noEmit
    git add src/events/bus.ts tests/events/bus-artifact-created.test.ts
    git commit -m "feat(v0.5.2.a): add artifact.created event"

---

## Task 3: `chunkMarkdown` Helper

**Files:**
- Create: `src/util/chunk-markdown.ts`
- Test: `tests/util/chunk-markdown.test.ts` (new)

- [ ] **Step 1: Write failing tests covering all 4 algorithm cases from spec §9**

Create `tests/util/chunk-markdown.test.ts`:

    import { describe, it, expect } from 'vitest';
    import { chunkMarkdown } from '../../src/util/chunk-markdown.js';

    describe('chunkMarkdown', () => {
      it('returns [] for empty string', () => {
        expect(chunkMarkdown('', 4096)).toEqual([]);
      });

      it('returns single chunk when text fits in maxChars', () => {
        const text = 'a'.repeat(4096);
        expect(chunkMarkdown(text, 4096)).toEqual([text]);
      });

      it('splits at paragraph boundary when available within maxChars', () => {
        const para1 = 'a'.repeat(3000);
        const para2 = 'b'.repeat(2000);
        const result = chunkMarkdown(`${para1}\n\n${para2}`, 4096);
        expect(result).toHaveLength(2);
        expect(result[0]).toBe(para1);
        expect(result[1]).toBe(para2);
      });

      it('splits at last whitespace within maxChars when no paragraph boundary', () => {
        const text = 'word '.repeat(1000);
        const result = chunkMarkdown(text, 4096);
        expect(result.length).toBeGreaterThanOrEqual(2);
        expect(result.every(chunk => chunk.length <= 4096)).toBe(true);
      });

      it('hard-splits mid-token at exactly maxChars when no whitespace', () => {
        const text = 'a'.repeat(5000);
        const result = chunkMarkdown(text, 4096);
        expect(result).toHaveLength(2);
        expect(result[0]).toHaveLength(4096);
        expect(result[1]).toHaveLength(904);
        expect(result[0] + result[1]).toBe(text);
      });

      it('every chunk satisfies length <= maxChars invariant', () => {
        const text = 'x'.repeat(50_000);
        const result = chunkMarkdown(text, 4096);
        for (const chunk of result) expect(chunk.length).toBeLessThanOrEqual(4096);
      });
    });

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/util/chunk-markdown.test.ts`. Expected: FAIL.

- [ ] **Step 3: Implement minimal helper**

Create `src/util/chunk-markdown.ts`:

    /**
     * Split a string into chunks no longer than maxChars each.
     *
     * Algorithm priority:
     *   1. If text fits → return [text].
     *   2. Try paragraph boundary (\n\n) at or before maxChars.
     *   3. Else try last whitespace at or before maxChars.
     *   4. Else hard split at exactly maxChars.
     *
     * Empty input returns [] (NOT [''])
     */
    export function chunkMarkdown(text: string, maxChars: number): string[] {
      if (text.length === 0) return [];
      if (text.length <= maxChars) return [text];

      const paraIdx = text.lastIndexOf('\n\n', maxChars);
      if (paraIdx > 0) {
        const head = text.slice(0, paraIdx);
        const tail = text.slice(paraIdx + 2);
        return [head, ...chunkMarkdown(tail, maxChars)];
      }

      const wsMatch = text.slice(0, maxChars).match(/\s(?=\S*$)/);
      if (wsMatch && wsMatch.index !== undefined && wsMatch.index > 0) {
        const splitAt = wsMatch.index;
        const head = text.slice(0, splitAt);
        const tail = text.slice(splitAt + 1);
        return [head, ...chunkMarkdown(tail, maxChars)];
      }

      const head = text.slice(0, maxChars);
      const tail = text.slice(maxChars);
      return [head, ...chunkMarkdown(tail, maxChars)];
    }

- [ ] **Step 4: Run tests, commit**

    npx vitest run tests/util/chunk-markdown.test.ts
    git add src/util/chunk-markdown.ts tests/util/chunk-markdown.test.ts
    git commit -m "feat(v0.5.2.a): add chunkMarkdown shared helper"

---

## Task 4: One-Line CustomProvider `.status` Attachment

**Files:**
- Modify: `src/worker/providers/custom.ts:41`
- Test: `tests/worker/custom-provider-error-status.test.ts` (new)

- [ ] **Step 1: Write failing test**

Create `tests/worker/custom-provider-error-status.test.ts`:

    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
    import { CustomProvider } from '../../src/worker/providers/custom.js';

    describe('CustomProvider error.status', () => {
      let originalFetch: typeof globalThis.fetch;
      beforeEach(() => { originalFetch = globalThis.fetch; });
      afterEach(() => { globalThis.fetch = originalFetch; });

      it('attaches .status when upstream returns non-OK', async () => {
        globalThis.fetch = vi.fn().mockResolvedValue({
          ok: false, status: 503, text: async () => 'service unavailable',
        } as Response);

        const provider = new CustomProvider('http://example.invalid', 'key');
        let caught: unknown;
        try {
          await provider.chat([{ role: 'user', content: 'x' }], { model: 'm', systemPrompt: '' });
        } catch (err) { caught = err; }

        expect(caught).toBeInstanceOf(Error);
        expect((caught as Error & { status?: number }).status).toBe(503);
      });
    });

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/worker/custom-provider-error-status.test.ts`. Expected: FAIL.

- [ ] **Step 3: Patch CustomProvider**

In `src/worker/providers/custom.ts`, locate the `if (!res.ok)` block and replace with:

    if (!res.ok) {
      const err = Object.assign(
        new Error(`Custom provider error: ${res.status} ${await res.text()}`),
        { status: res.status },
      );
      throw err;
    }

- [ ] **Step 4: Verify tests pass and commit**

    npx vitest run tests/worker/custom-provider-error-status.test.ts
    npx vitest run tests/worker/
    git add src/worker/providers/custom.ts tests/worker/custom-provider-error-status.test.ts
    git commit -m "fix(v0.5.2.a): attach .status to CustomProvider thrown errors"

---

## Task 5: Artifact Error Classes

**Files:**
- Create: `src/council/artifact-errors.ts`
- Test: `tests/council/artifact-errors.test.ts` (new)

- [ ] **Step 1: Write failing test**

Create `tests/council/artifact-errors.test.ts`:

    import { describe, it, expect } from 'vitest';
    import {
      MissingSynthesizerConfigError,
      ArtifactEmptySegmentError,
      SynthesisAlreadyRunningError,
      ArtifactResetInFlightError,
      ArtifactDeliberationInFlightError,
      PendingClassificationError,
      ArtifactBlindReviewActiveError,
      MalformedArtifactError,
      EmptyResponseError,
      ProviderTimeoutError,
      SynthesisRetryExhaustedError,
    } from '../../src/council/artifact-errors.js';

    describe('artifact errors', () => {
      it('all classes extend Error and have unique names', () => {
        const instances = [
          new MissingSynthesizerConfigError(),
          new ArtifactEmptySegmentError(),
          new SynthesisAlreadyRunningError(42),
          new ArtifactResetInFlightError(42),
          new ArtifactDeliberationInFlightError(42),
          new PendingClassificationError(42),
          new ArtifactBlindReviewActiveError(42),
          new MalformedArtifactError('raw'),
          new EmptyResponseError(),
          new ProviderTimeoutError(30000),
          new SynthesisRetryExhaustedError(new Error('inner')),
        ];
        const names = instances.map(e => e.name);
        expect(new Set(names).size).toBe(names.length);
        for (const e of instances) expect(e).toBeInstanceOf(Error);
      });

      it('ProviderTimeoutError carries timeoutMs', () => {
        const e = new ProviderTimeoutError(30000);
        expect(e.timeoutMs).toBe(30000);
      });

      it('SynthesisRetryExhaustedError wraps inner cause', () => {
        const inner = new Error('boom');
        const e = new SynthesisRetryExhaustedError(inner);
        expect(e.cause).toBe(inner);
      });

      it('MalformedArtifactError carries raw response', () => {
        const e = new MalformedArtifactError('no TL;DR here');
        expect(e.rawResponse).toBe('no TL;DR here');
      });
    });

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/council/artifact-errors.test.ts`. Expected: FAIL.

- [ ] **Step 3: Create error module**

Create `src/council/artifact-errors.ts` with the 11 error classes (each `extends Error`, sets `this.name`, optional payload fields):

    /**
     * Error taxonomy for /councildone synthesis pipeline.
     * Names are artifact-prefixed where there is a clash with session-reset's
     * existing errors so adapters can distinguish without import aliasing.
     */

    export class MissingSynthesizerConfigError extends Error {
      constructor() {
        super('No agent has role_type: artifact-synthesizer');
        this.name = 'MissingSynthesizerConfigError';
      }
    }

    export class ArtifactEmptySegmentError extends Error {
      constructor() {
        super('Current segment has no messages');
        this.name = 'ArtifactEmptySegmentError';
      }
    }

    export class SynthesisAlreadyRunningError extends Error {
      constructor(public threadId: number) {
        super(`Synthesis already in flight for thread ${threadId}`);
        this.name = 'SynthesisAlreadyRunningError';
      }
    }

    export class ArtifactResetInFlightError extends Error {
      constructor(public threadId: number) {
        super(`Reset in flight for thread ${threadId}; retry /councildone after reset completes`);
        this.name = 'ArtifactResetInFlightError';
      }
    }

    export class ArtifactDeliberationInFlightError extends Error {
      constructor(public threadId: number) {
        super(`Deliberation in flight for thread ${threadId}; retry /councildone after the round ends`);
        this.name = 'ArtifactDeliberationInFlightError';
      }
    }

    export class PendingClassificationError extends Error {
      constructor(public threadId: number) {
        super(`Message classification pending for thread ${threadId}; retry /councildone shortly`);
        this.name = 'PendingClassificationError';
      }
    }

    export class ArtifactBlindReviewActiveError extends Error {
      constructor(public threadId: number) {
        super(`Blind review active for thread ${threadId}; resolve via /blindreview reveal or /cancelreview`);
        this.name = 'ArtifactBlindReviewActiveError';
      }
    }

    export class MalformedArtifactError extends Error {
      constructor(public rawResponse: string) {
        super('Synthesizer output missing required ## TL;DR section');
        this.name = 'MalformedArtifactError';
      }
    }

    export class EmptyResponseError extends Error {
      constructor() {
        super('Synthesizer returned empty content (or provider empty-rewrite sentinel)');
        this.name = 'EmptyResponseError';
      }
    }

    export class ProviderTimeoutError extends Error {
      constructor(public timeoutMs: number) {
        super(`Provider call exceeded ${timeoutMs}ms`);
        this.name = 'ProviderTimeoutError';
      }
    }

    export class SynthesisRetryExhaustedError extends Error {
      constructor(public cause: unknown) {
        super(`Synthesis failed after retries; cause: ${cause instanceof Error ? cause.message : String(cause)}`);
        this.name = 'SynthesisRetryExhaustedError';
      }
    }

- [ ] **Step 4: Run tests, commit**

    npx vitest run tests/council/artifact-errors.test.ts
    git add src/council/artifact-errors.ts tests/council/artifact-errors.test.ts
    git commit -m "feat(v0.5.2.a): add artifact synthesis error taxonomy"

---

## Task 6: `ArtifactDB` Layer

**Files:**
- Create: `src/council/artifact-db.ts`
- Test: `tests/council/artifact-db.test.ts` (new)

- [ ] **Step 1: Write failing tests** covering insert + findByThreadPreset (latest-row contract) + findByThread + maxThreadLocalSeq + deleteById + UNIQUE constraints + fetchByThreadLocalSeq (thread-scoped)

Create `tests/council/artifact-db.test.ts` with the following describe blocks (full assertions in spec §11):

- `insert + findByThreadPreset` — insert one row, verify findable; insert THREE rows with mixed segment_index, verify findByThreadPreset returns the row with HIGHEST segment_index (the latest-row contract from spec §4)
- `findByThread` — multiple presets in one thread, scoped by thread_id
- `maxThreadLocalSeq` — null when empty; max value when populated
- `deleteById` — round trip
- `UNIQUE constraints` — duplicate (thread_id, segment_index) rejected; duplicate (thread_id, thread_local_seq) rejected
- `fetchByThreadLocalSeq` — thread-scoped (id 7 in thread 1 NOT visible from thread 2)

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/council/artifact-db.test.ts`. Expected: FAIL.

- [ ] **Step 3: Implement ArtifactDB**

Create `src/council/artifact-db.ts` mirroring the shape of `src/storage/reset-snapshot-db.ts`:

    import Database from 'better-sqlite3';
    import { mkdirSync } from 'node:fs';
    import { dirname } from 'node:path';

    export interface ArtifactRow {
      id: number;
      thread_id: number;
      segment_index: number;
      thread_local_seq: number;
      preset: 'universal' | 'decision';
      content_md: string;
      created_at: string;
      synthesis_model: string | null;
      synthesis_token_usage_json: string | null;
    }

    export interface ArtifactInsertInput {
      thread_id: number;
      segment_index: number;
      thread_local_seq: number;
      preset: 'universal' | 'decision';
      content_md: string;
      created_at: string;
      synthesis_model?: string | null;
      synthesis_token_usage_json?: string | null;
    }

    export class ArtifactDB {
      private db: Database.Database;

      constructor(dbPath: string) {
        if (dbPath !== ':memory:') {
          mkdirSync(dirname(dbPath), { recursive: true });
        }
        this.db = new Database(dbPath);
        this.db.pragma('journal_mode = WAL');
        this.migrate();
      }

      private migrate(): void {
        this.db.exec(`
          CREATE TABLE IF NOT EXISTS council_artifacts (
            id                          INTEGER PRIMARY KEY AUTOINCREMENT,
            thread_id                   INTEGER NOT NULL,
            segment_index               INTEGER NOT NULL,
            thread_local_seq            INTEGER NOT NULL,
            preset                      TEXT NOT NULL,
            content_md                  TEXT NOT NULL,
            created_at                  TEXT NOT NULL,
            synthesis_model             TEXT,
            synthesis_token_usage_json  TEXT,
            UNIQUE(thread_id, segment_index),
            UNIQUE(thread_id, thread_local_seq)
          );
          CREATE INDEX IF NOT EXISTS idx_artifacts_thread
            ON council_artifacts(thread_id, created_at DESC);
        `);
      }

      insert(input: ArtifactInsertInput): ArtifactRow {
        const result = this.db
          .prepare(
            `INSERT INTO council_artifacts
               (thread_id, segment_index, thread_local_seq, preset, content_md, created_at, synthesis_model, synthesis_token_usage_json)
             VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
          )
          .run(
            input.thread_id, input.segment_index, input.thread_local_seq,
            input.preset, input.content_md, input.created_at,
            input.synthesis_model ?? null, input.synthesis_token_usage_json ?? null,
          );
        const id = Number(result.lastInsertRowid);
        return this.fetchById(id)!;
      }

      /**
       * Returns LATEST artifact for (thread_id, preset) ordered by segment_index DESC.
       * Latest-by-segment_index is contractually required for cache invalidation.
       */
      findByThreadPreset(threadId: number, preset: 'universal' | 'decision'): ArtifactRow | null {
        const row = this.db
          .prepare(
            `SELECT * FROM council_artifacts
             WHERE thread_id = ? AND preset = ?
             ORDER BY segment_index DESC
             LIMIT 1`,
          )
          .get(threadId, preset) as ArtifactRow | undefined;
        return row ?? null;
      }

      findByThread(threadId: number): ArtifactRow[] {
        return this.db
          .prepare(`SELECT * FROM council_artifacts WHERE thread_id = ? ORDER BY segment_index ASC`)
          .all(threadId) as ArtifactRow[];
      }

      maxThreadLocalSeq(threadId: number): number | null {
        const row = this.db
          .prepare(`SELECT MAX(thread_local_seq) AS m FROM council_artifacts WHERE thread_id = ?`)
          .get(threadId) as { m: number | null } | undefined;
        return row?.m ?? null;
      }

      deleteById(id: number): void {
        this.db.prepare(`DELETE FROM council_artifacts WHERE id = ?`).run(id);
      }

      fetchById(id: number): ArtifactRow | null {
        const row = this.db
          .prepare(`SELECT * FROM council_artifacts WHERE id = ?`)
          .get(id) as ArtifactRow | undefined;
        return row ?? null;
      }

      fetchByThreadLocalSeq(threadId: number, seq: number): ArtifactRow | null {
        const row = this.db
          .prepare(`SELECT * FROM council_artifacts WHERE thread_id = ? AND thread_local_seq = ?`)
          .get(threadId, seq) as ArtifactRow | undefined;
        return row ?? null;
      }
    }

- [ ] **Step 4: Run tests, commit**

    npx vitest run tests/council/artifact-db.test.ts
    git add src/council/artifact-db.ts tests/council/artifact-db.test.ts
    git commit -m "feat(v0.5.2.a): add ArtifactDB sqlite layer for council_artifacts table"

---

## Task 7: `segment-counter` Cross-Module Helper

**Files:**
- Create: `src/council/segment-counter.ts`
- Test: `tests/council/segment-counter.test.ts` (new)

- [ ] **Step 1: Write failing tests**

Create `tests/council/segment-counter.test.ts` testing:

- fresh-thread fallback (handler.getSegments.length - 1) when both tables empty
- max(existing) + 1 when only reset table has rows
- max(existing) + 1 when only artifact table has rows
- max across BOTH tables when both populated
- thread scoping (other thread's segments don't affect result)

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/council/segment-counter.test.ts`. Expected: FAIL.

- [ ] **Step 3: Implement helper**

Create `src/council/segment-counter.ts`:

    import type { ResetSnapshotDB } from '../storage/reset-snapshot-db.js';
    import type { ArtifactDB } from './artifact-db.js';

    interface HandlerForCounter {
      getSegments(threadId: number): readonly { snapshotId: string | null }[];
    }

    /**
     * Cross-table seal-counter formula. Single source of truth used by BOTH
     * /councildone (ArtifactService) and /councilreset (SessionReset) so the
     * counter stays monotonic across process restarts.
     *
     * Spec §4: nextSegmentIndex = max(reset_snapshots ∪ council_artifacts) + 1
     *                          OR handler.getSegments(threadId).length - 1
     */
    export function computeNextSegmentIndex(
      threadId: number,
      resetDb: ResetSnapshotDB,
      artifactDb: ArtifactDB,
      handler: HandlerForCounter,
    ): number {
      const resetIdx = resetDb.listSnapshotsForThread(threadId).map(s => s.segmentIndex);
      const artIdx = artifactDb.findByThread(threadId).map(r => r.segment_index);
      const all = [...resetIdx, ...artIdx];
      if (all.length > 0) return Math.max(...all) + 1;
      return handler.getSegments(threadId).length - 1;
    }

- [ ] **Step 4: Run tests, commit**

    npx vitest run tests/council/segment-counter.test.ts
    git add src/council/segment-counter.ts tests/council/segment-counter.test.ts
    git commit -m "feat(v0.5.2.a): add cross-table segment-counter helper"

---

## Task 8: `artifact-prompt` Builder + `parseArtifact`

**Files:**
- Create: `src/council/artifact-prompt.ts`
- Test: `tests/council/artifact-prompt.test.ts` (new)

- [ ] **Step 1: Write failing tests**

Create `tests/council/artifact-prompt.test.ts`. Key cases:

- `buildArtifactPrompt`: returns `{messages, options}` where `options.systemPromptParts` is `undefined` (skips personality.ts ban); `systemPrompt` mentions required headings per preset; low temperature 0.2-0.3; explicit `maxTokens: 3000`
- `parseArtifact`: extracts TL;DR with another heading following
- `parseArtifact`: extracts TL;DR at end-of-string (no trailing heading)
- `parseArtifact`: TL;DR containing uppercase Z is NOT truncated (regression test for `\Z` anchor bug — JS regex has no `\Z`)
- `parseArtifact`: returns `{tldr: null}` when `## TL;DR` heading missing
- `parseArtifact`: matches `## TL;DR` at very start (no leading newline)

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/council/artifact-prompt.test.ts`. Expected: FAIL.

- [ ] **Step 3: Implement prompt builder + parser**

Create `src/council/artifact-prompt.ts`:

    import type { CouncilMessage, ProviderMessage, ChatOptions } from '../types.js';

    export type Preset = 'universal' | 'decision';

    const UNIVERSAL_SYSTEM = `You are an artifact synthesizer. Read the council deliberation transcript and produce a structured markdown decision memo.

    REQUIRED structure (in this exact order, with these exact headings):

    ## TL;DR

    A 2-3 sentence conclusion of the discussion.

    ## Discussion

    Summarize key points raised during deliberation.

    ## Open questions

    List unresolved questions that surfaced.

    ## Suggested next step

    One concrete action to take next.

    The ## TL;DR section is mandatory. Output ONLY this markdown — no preamble, no commentary.`;

    const DECISION_SYSTEM = `You are an artifact synthesizer. Read the council deliberation transcript and produce a structured decision memo.

    REQUIRED structure (in this exact order, with these exact headings):

    ## TL;DR

    A 2-3 sentence conclusion stating the recommended decision.

    ## Options considered

    List the options that were debated.

    ## Recommended option

    State the recommendation and one-line justification.

    ## Trade-offs

    What is sacrificed by the recommendation.

    ## Suggested next step

    One concrete action to take next.

    The ## TL;DR section is mandatory. Output ONLY this markdown — no preamble, no commentary.`;

    export function buildArtifactPrompt(
      preset: Preset,
      transcript: readonly CouncilMessage[],
      modelName: string,
    ): { messages: ProviderMessage[]; options: ChatOptions } {
      const systemPrompt = preset === 'decision' ? DECISION_SYSTEM : UNIVERSAL_SYSTEM;
      const transcriptBody = transcript.map(t => `[${t.agentId}] ${t.content}`).join('\n\n');

      const messages: ProviderMessage[] = [
        { role: 'user', content: `Council deliberation transcript:\n\n${transcriptBody}\n\nProduce the artifact now.` },
      ];

      const options: ChatOptions = {
        model: modelName,
        systemPrompt,
        maxTokens: 3000,
        temperature: 0.25,
        // NO systemPromptParts: keeps personality.ts markdown ban OUT of this call.
      };

      return { messages, options };
    }

    /**
     * Extract the TL;DR content from a synthesizer artifact.
     *
     * Spec §5: regex avoids \Z (not a JS anchor) and the m flag (which would
     * make $ a line-end and stop at the first newline). Uses (?:^|\n) for
     * line-start semantics and bare $ for end-of-string.
     */
    const TLDR_RE = /(?:^|\n)## TL;DR\s*\n+([^\n][\s\S]*?)(?=\n## |$)/;

    export function parseArtifact(content: string): { tldr: string | null } {
      const m = content.match(TLDR_RE);
      if (!m) return { tldr: null };
      return { tldr: m[1].trim() };
    }

- [ ] **Step 4: Run tests, commit**

    npx vitest run tests/council/artifact-prompt.test.ts
    git add src/council/artifact-prompt.ts tests/council/artifact-prompt.test.ts
    git commit -m "feat(v0.5.2.a): add artifact prompt builder + TL;DR parser"

---

## Task 9: `artifact-invoke` Timeout + Retry + Empty-Sentinel Detection

**Files:**
- Create: `src/council/artifact-invoke.ts`
- Test: `tests/council/artifact-invoke.test.ts` (new)

- [ ] **Step 1: Write failing tests**

Create `tests/council/artifact-invoke.test.ts`. Key cases:

`invokeProviderForArtifact`:
- resolves provider response when chat finishes within timeout
- rejects with `ProviderTimeoutError` when chat exceeds timeout
- clears the scheduled timer on success path (verify via `vi.spyOn(globalThis, 'setTimeout')` and `clearTimeout`)

`isProviderEmptyResponse`:
- matches literal empty + whitespace-only
- matches OpenAI sentinel `（model 未回傳內容，finish_reason: ...）`
- does NOT match real markdown content with parens

`isHardFail`:
- `ProviderTimeoutError` → false (retry)
- `EmptyResponseError` → false (retry)
- `.status === 429` or 5xx → false (retry)
- `.status === 401/404` (4xx non-429) → true (hard)
- plain `Error` without `.status` → false (retry conservatively per spec §8)

`invokeWithRetry`:
- returns response on first success (1 invocation)
- OpenAI sentinel on attempt 1, valid markdown on attempt 2 → returns valid (2 invocations) — regression test for round-10 P2-3
- 401 hard-fails immediately (1 invocation)
- plain `Error` on every attempt → throws `SynthesisRetryExhaustedError` after 4 attempts

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/council/artifact-invoke.test.ts`. Expected: FAIL.

- [ ] **Step 3: Implement invoke + retry**

Create `src/council/artifact-invoke.ts`:

    import type { LLMProvider, ProviderMessage, ProviderResponse, ChatOptions } from '../types.js';
    import {
      ProviderTimeoutError, EmptyResponseError, SynthesisRetryExhaustedError,
    } from './artifact-errors.js';

    const PER_ATTEMPT_TIMEOUT_MS = 30_000;
    const MAX_ATTEMPTS = 4;
    const SLEEPS_MS = [1000, 2000, 4000];

    function sleep(ms: number): Promise<void> {
      return new Promise(r => setTimeout(r, ms));
    }

    /**
     * Caller-side timeout via Promise.race. Underlying fetch is NOT cancelled.
     *
     * KNOWN LIMITATION (spec §5): under retry storm with hung provider, up to
     * MAX_ATTEMPTS concurrent in-flight LLM calls can accumulate. Caller-visible
     * bound is ~127s but token cost can be ~4× per attempt budget. v0.5.3+
     * AbortSignal threading is the real fix.
     */
    export async function invokeProviderForArtifact(
      provider: LLMProvider,
      messages: ProviderMessage[],
      options: ChatOptions,
      perAttemptTimeoutMs: number = PER_ATTEMPT_TIMEOUT_MS,
    ): Promise<ProviderResponse> {
      let timeoutHandle: ReturnType<typeof setTimeout> | undefined;
      try {
        return await Promise.race([
          provider.chat(messages, options),
          new Promise<never>((_, reject) => {
            timeoutHandle = setTimeout(
              () => reject(new ProviderTimeoutError(perAttemptTimeoutMs)),
              perAttemptTimeoutMs,
            );
          }),
        ]);
      } finally {
        if (timeoutHandle !== undefined) clearTimeout(timeoutHandle);
      }
    }

    /** OpenAI provider rewrites empty completions into this Chinese sentinel. */
    const OPENAI_EMPTY_SENTINEL = /^（[^（）]+未回傳內容，finish_reason:\s*[^）]+）\s*$/;

    export function isProviderEmptyResponse(content: string): boolean {
      if (!content || content.trim().length === 0) return true;
      if (OPENAI_EMPTY_SENTINEL.test(content.trim())) return true;
      return false;
    }

    export function isHardFail(err: unknown): boolean {
      if (err instanceof ProviderTimeoutError) return false;
      if (err instanceof EmptyResponseError) return false;
      if (err instanceof Error && 'status' in err) {
        const status = (err as { status: number }).status;
        if (status === 429) return false;
        if (status >= 500 && status < 600) return false;
        if (status >= 400 && status < 500) return true;
      }
      return false;
    }

    export async function invokeWithRetry(
      provider: LLMProvider,
      messages: ProviderMessage[],
      options: ChatOptions,
    ): Promise<ProviderResponse> {
      let lastErr: unknown;
      for (let attempt = 1; attempt <= MAX_ATTEMPTS; attempt++) {
        if (attempt > 1) await sleep(SLEEPS_MS[attempt - 2]);
        try {
          const response = await invokeProviderForArtifact(
            provider, messages, options, PER_ATTEMPT_TIMEOUT_MS,
          );
          if (isProviderEmptyResponse(response.content)) {
            lastErr = new EmptyResponseError();
            continue;
          }
          return response;
        } catch (err) {
          lastErr = err;
          if (isHardFail(err)) throw err;
        }
      }
      throw new SynthesisRetryExhaustedError(lastErr);
    }

- [ ] **Step 4: Run tests, commit**

    npx vitest run tests/council/artifact-invoke.test.ts
    git add src/council/artifact-invoke.ts tests/council/artifact-invoke.test.ts
    git commit -m "feat(v0.5.2.a): add artifact-invoke timeout + retry + sentinel detection"

---

## Task 10: Extend `DeliberationHandler`

**Files:**
- Modify: `src/council/deliberation.ts`
- Test: `tests/council/deliberation-synthesis-flag.test.ts` (new)

- [ ] **Step 1: Read the existing `tests/council/deliberation-segments.test.ts` factory pattern**

Open it, copy the `beforeEach` setup that constructs `DeliberationHandler` with mock dependencies. Use that pattern in the new test file.

- [ ] **Step 2: Write failing tests**

Create `tests/council/deliberation-synthesis-flag.test.ts` testing:

- `isSynthesisInFlight(threadId)` defaults to `false` for any thread
- `setSynthesisInFlight(t, true)` is observable via `isSynthesisInFlight(t)`; per-thread scope (thread 1 set does not affect thread 2)
- `setSynthesisInFlight(t, false)` clears
- `unsealCurrentSegment(threadId)` reverts the most recent in-memory seal (verify via `getSegments(t).length` round trip)
- `runDeliberation` rejects when `isSynthesisInFlight(threadId)` is true (parallel to existing `resetInFlight` guard)

- [ ] **Step 3: Run tests to verify they fail**

Run: `npx vitest run tests/council/deliberation-synthesis-flag.test.ts`. Expected: FAIL.

- [ ] **Step 4: Add private field + accessors + unseal method**

In `src/council/deliberation.ts`, find the existing `resetInFlight` field and add nearby:

    private synthesisInFlight = new Set<number>();

    public setSynthesisInFlight(threadId: number, value: boolean): void {
      if (value) this.synthesisInFlight.add(threadId);
      else this.synthesisInFlight.delete(threadId);
    }

    public isSynthesisInFlight(threadId: number): boolean {
      return this.synthesisInFlight.has(threadId);
    }

    /**
     * Reverts the most recent in-memory seal (used by ArtifactService rollback
     * when DB insert or openNewSegment throws after seal). Mirrors the manual
     * rollback pattern at session-reset.ts:190.
     */
    public unsealCurrentSegment(threadId: number): void {
      const segments = this.threadSegments.get(threadId);
      if (!segments || segments.length === 0) return;
      segments.pop();
    }

(Substitute the actual per-thread segments map field name — `threadSegments` is a placeholder. Find it by reading the class.)

- [ ] **Step 5: Extend `sealCurrentSegment` signature to accept `string | null`**

Find existing `sealCurrentSegment` method. Change parameter type from `string` to `string | null`. Existing `SessionReset` callers pass a real snapshotId; new `ArtifactService` callers pass `null` (sealed by /councildone).

- [ ] **Step 6: Add `runDeliberation` guard against synthesisInFlight**

Find start of `runDeliberation` (mirror the existing `resetInFlight` guard location). Add:

    if (this.isSynthesisInFlight(threadId)) {
      throw new Error('synthesis in flight; please wait');
    }

(Promote to typed error matching existing reset guard style if preferred.)

- [ ] **Step 7: Run tests, full council suite, commit**

    npx vitest run tests/council/deliberation-synthesis-flag.test.ts tests/council/deliberation.test.ts
    npx vitest run tests/council/
    git add src/council/deliberation.ts tests/council/deliberation-synthesis-flag.test.ts
    git commit -m "feat(v0.5.2.a): add synthesisInFlight + unsealCurrentSegment to DeliberationHandler"

---

## Task 11: `ArtifactService.synthesize` Phase 1 (Pre-Checks Only)

**Files:**
- Create: `src/council/artifact-service.ts`
- Test: `tests/council/artifact-service-precheck.test.ts` (new)

This task implements ONLY Phase 1 pre-checks (no synthesis, no commit). Phase 2/3 land in Task 12.

- [ ] **Step 1: Write failing tests for Phase 1 guards**

Create `tests/council/artifact-service-precheck.test.ts` covering:

- `MissingSynthesizerConfigError` when `synthesizerConfig === null` — FIRST check, fires even when other guards would also trigger
- Each transient guard throws its specific error (`ArtifactResetInFlightError`, `ArtifactDeliberationInFlightError`, `PendingClassificationError`)
- Persistent guard `ArtifactBlindReviewActiveError` when blind-review session active
- `ArtifactEmptySegmentError` when current segment empty AND no fast-path applicable
- `SynthesisAlreadyRunningError` when `handler.isSynthesisInFlight()` is true
- Cached fast-path return when: cached row exists AND `cached.segment_index === lastSealedSegmentIndex` AND `currentSegment.messages.length === 0`

Use a mock handler factory; mock provider not needed for pre-check tests.

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/council/artifact-service-precheck.test.ts`. Expected: FAIL.

- [ ] **Step 3: Implement Phase 1 of ArtifactService**

Create `src/council/artifact-service.ts`:

    import type { AgentConfig, CouncilMessage } from '../types.js';
    import type { CouncilEventBus } from '../events/bus.js';
    import type { ResetSnapshotDB } from '../storage/reset-snapshot-db.js';
    import { ArtifactDB, type ArtifactRow } from './artifact-db.js';
    import {
      MissingSynthesizerConfigError,
      ArtifactResetInFlightError,
      ArtifactDeliberationInFlightError,
      PendingClassificationError,
      ArtifactBlindReviewActiveError,
      ArtifactEmptySegmentError,
      SynthesisAlreadyRunningError,
      MalformedArtifactError,
    } from './artifact-errors.js';
    import { computeNextSegmentIndex } from './segment-counter.js';
    import { buildArtifactPrompt, parseArtifact, type Preset } from './artifact-prompt.js';
    import { invokeWithRetry } from './artifact-invoke.js';
    import { createProvider } from '../worker/providers/factory.js';

    export interface HandlerForArtifact {
      isResetInFlight(threadId: number): boolean;
      isDeliberationInFlight(threadId: number): boolean;
      hasPendingClassifications(threadId: number): boolean;
      getBlindReviewSessionId(threadId: number): string | null;
      getCurrentSegment(threadId: number): { messages: readonly CouncilMessage[] };
      getSegments(threadId: number): readonly { snapshotId: string | null }[];
      isSynthesisInFlight(threadId: number): boolean;
      setSynthesisInFlight(threadId: number, value: boolean): void;
      sealCurrentSegment(threadId: number, snapshotId: string | null): void;
      unsealCurrentSegment(threadId: number): void;
      openNewSegment(threadId: number): void;
    }

    export interface ArtifactServiceDeps {
      synthesizerConfig: AgentConfig | null;
      artifactDb: ArtifactDB;
      resetDb: ResetSnapshotDB;
      handler: HandlerForArtifact;
      bus: CouncilEventBus;
    }

    export class ArtifactService {
      constructor(private deps: ArtifactServiceDeps) {}

      lastSealedSegmentIndex(threadId: number): number | null {
        const r = this.deps.resetDb.listSnapshotsForThread(threadId).map(s => s.segmentIndex);
        const a = this.deps.artifactDb.findByThread(threadId).map(x => x.segment_index);
        const all = [...r, ...a];
        if (all.length === 0) return null;
        return Math.max(...all);
      }

      async synthesize(threadId: number, preset: Preset): Promise<ArtifactRow> {
        // === Phase 1: pre-checks (no mutation) ===

        // FIRST: missing-config — fires even when other guards would also trigger.
        if (this.deps.synthesizerConfig === null) {
          throw new MissingSynthesizerConfigError();
        }

        // Transient locks.
        if (this.deps.handler.isResetInFlight(threadId)) throw new ArtifactResetInFlightError(threadId);
        if (this.deps.handler.isDeliberationInFlight(threadId)) throw new ArtifactDeliberationInFlightError(threadId);
        if (this.deps.handler.hasPendingClassifications(threadId)) throw new PendingClassificationError(threadId);
        // Persistent guard.
        if (this.deps.handler.getBlindReviewSessionId(threadId)) throw new ArtifactBlindReviewActiveError(threadId);

        const currentSegment = this.deps.handler.getCurrentSegment(threadId);

        // Fast-path (after guards, so messages.length === 0 is authoritative).
        const cached = this.deps.artifactDb.findByThreadPreset(threadId, preset);
        const lastSealedIdx = this.lastSealedSegmentIndex(threadId);
        if (
          cached &&
          lastSealedIdx !== null &&
          cached.segment_index === lastSealedIdx &&
          currentSegment.messages.length === 0
        ) {
          return cached;
        }

        if (currentSegment.messages.length === 0) throw new ArtifactEmptySegmentError();
        if (this.deps.handler.isSynthesisInFlight(threadId)) throw new SynthesisAlreadyRunningError(threadId);

        // Phase 2 + 3 are added in Task 12.
        throw new Error('Phase 2/3 not yet implemented (Task 12)');
      }

      fetchByThreadLocalSeq(threadId: number, seq: number): ArtifactRow | null {
        return this.deps.artifactDb.fetchByThreadLocalSeq(threadId, seq);
      }
    }

- [ ] **Step 4: Run tests, commit**

    npx vitest run tests/council/artifact-service-precheck.test.ts
    git add src/council/artifact-service.ts tests/council/artifact-service-precheck.test.ts
    git commit -m "feat(v0.5.2.a): ArtifactService phase 1 pre-checks + fast-path"

---

## Task 12: `ArtifactService.synthesize` Phase 2 + 3 (Synthesis + Commit + Rollback)

**Files:**
- Modify: `src/council/artifact-service.ts`
- Test: `tests/council/artifact-service-synthesis.test.ts` (new)

- [ ] **Step 1: Write failing tests** for synthesis + commit + rollback

Create `tests/council/artifact-service-synthesis.test.ts` covering:

- Happy path: provider call → parse → seal → insert → openNewSegment → emit `artifact.created`. Verify `synthesisInFlight` cleared at end.
- `MalformedArtifactError` without retry when `## TL;DR` missing — segment NOT sealed, no row inserted.
- DB INSERT throws → manual `unsealCurrentSegment` rollback; `openNewSegment` NOT called.
- `openNewSegment` throws → both `deleteById` AND `unsealCurrentSegment` called best-effort; original `openErr` propagates.
- Best-effort rollback: if both `deleteById` AND `unsealCurrentSegment` cleanup steps throw, both errors are logged via `console.error`, original `openErr` still surfaces.
- `synthesisInFlight` cleared on every exit path (success, hard fail, rollback).

Use `vi.spyOn(factoryModule, 'createProvider')` to inject mock provider returning canned artifact body.

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/council/artifact-service-synthesis.test.ts`. Expected: FAIL (Phase 2/3 stub throws).

- [ ] **Step 3: Replace Phase 2/3 stub**

In `src/council/artifact-service.ts`, replace the `throw new Error('Phase 2/3 not yet implemented (Task 12)')` line with:

        // === Phase 2: synthesis (no segment mutation) ===
        this.deps.handler.setSynthesisInFlight(threadId, true);
        try {
          const provider = createProvider(this.deps.synthesizerConfig.provider);
          const { messages, options } = buildArtifactPrompt(
            preset,
            currentSegment.messages,
            this.deps.synthesizerConfig.model,
          );
          const response = await invokeWithRetry(provider, messages, options);

          const parsed = parseArtifact(response.content);
          if (!parsed.tldr) throw new MalformedArtifactError(response.content);

          // === Phase 3: commit ===
          const newSegmentIndex = computeNextSegmentIndex(
            threadId, this.deps.resetDb, this.deps.artifactDb, this.deps.handler,
          );
          const newSeq = (this.deps.artifactDb.maxThreadLocalSeq(threadId) ?? 0) + 1;

          this.deps.handler.sealCurrentSegment(threadId, null);

          let inserted: ArtifactRow;
          try {
            inserted = this.deps.artifactDb.insert({
              thread_id: threadId,
              segment_index: newSegmentIndex,
              thread_local_seq: newSeq,
              preset,
              content_md: response.content,
              created_at: new Date().toISOString(),
              synthesis_model: response.modelUsed ?? options.model,
              synthesis_token_usage_json: JSON.stringify(response.tokensUsed),
            });
          } catch (insertErr) {
            this.deps.handler.unsealCurrentSegment(threadId);
            throw insertErr;
          }

          try {
            this.deps.handler.openNewSegment(threadId);
          } catch (openErr) {
            // Best-effort rollback (round-8 P2-3).
            try { this.deps.artifactDb.deleteById(inserted.id); }
            catch (delErr) { console.error('[ArtifactService] rollback: deleteById failed', delErr); }
            try { this.deps.handler.unsealCurrentSegment(threadId); }
            catch (unsealErr) { console.error('[ArtifactService] rollback: unsealCurrentSegment failed', unsealErr); }
            throw openErr;
          }

          this.deps.bus.emit('artifact.created', {
            threadId,
            segmentIndex: newSegmentIndex,
            threadLocalSeq: newSeq,
            preset,
          });

          return inserted;
        } finally {
          this.deps.handler.setSynthesisInFlight(threadId, false);
        }

(`response.modelUsed` may not exist on `ProviderResponse`. Open `src/types.ts` and substitute the actual field name; if no model-back is provided by the provider response, fall back to `options.model`.)

- [ ] **Step 4: Run tests, commit**

    npx vitest run tests/council/artifact-service-synthesis.test.ts tests/council/artifact-service-precheck.test.ts
    git add src/council/artifact-service.ts tests/council/artifact-service-synthesis.test.ts
    git commit -m "feat(v0.5.2.a): ArtifactService phase 2/3 — synthesis + commit + rollback"

---

## Task 13: SessionReset Cross-Table Counter + synthesisInFlight Guard

**Files:**
- Modify: `src/council/session-reset.ts`
- Modify: `src/council/session-reset-errors.ts`
- Test: `tests/council/session-reset-synthesis-guard.test.ts` (new)

- [ ] **Step 1: Read the existing `tests/council/session-reset.test.ts` factory pattern**

Copy its mock handler and `SessionReset` construction; new tests inject an in-memory `ArtifactDB`.

- [ ] **Step 2: Write failing tests**

Create `tests/council/session-reset-synthesis-guard.test.ts` covering:

- Cross-table counter: `ArtifactDB` has segment_index = 5 for thread 42, no reset snapshots, in-memory state empty (simulates post-restart). `SessionReset.reset` on a non-empty current segment in thread 42 → persisted snapshot has `segment_index = 6` (NOT 0, NOT 1).
- `SynthesisInProgressError` thrown when `handler.isSynthesisInFlight(threadId)` is true.

- [ ] **Step 3: Run tests to verify they fail**

Run: `npx vitest run tests/council/session-reset-synthesis-guard.test.ts`. Expected: FAIL.

- [ ] **Step 4: Update SessionReset**

In `src/council/session-reset.ts`:

1. Add imports:

    import type { ArtifactDB } from './artifact-db.js';
    import { computeNextSegmentIndex } from './segment-counter.js';

2. Update `HandlerForReset`:

    export interface HandlerForReset {
      // ... existing methods ...
      isSynthesisInFlight(threadId: number): boolean;
    }

3. Update constructor:

    export class SessionReset {
      constructor(
        private db: ResetSnapshotDB,
        private artifactDb: ArtifactDB,
        private facilitator: FacilitatorForReset,
      ) {}

4. Add synthesis guard near other transient guards:

    if (handler.isSynthesisInFlight(threadId)) {
      throw new SynthesisInProgressError(threadId);
    }

5. Replace the existing line-172 segment-index calculation:

    const segmentIndex = computeNextSegmentIndex(threadId, this.db, this.artifactDb, handler);

- [ ] **Step 5: Add `SynthesisInProgressError` to session-reset-errors.ts**

Append:

    export class SynthesisInProgressError extends Error {
      constructor(public threadId: number) {
        super(`Synthesis in progress for thread ${threadId}; retry /councilreset shortly`);
        this.name = 'SynthesisInProgressError';
      }
    }

- [ ] **Step 6: Run tests, commit**

    npx vitest run tests/council/session-reset.test.ts tests/council/session-reset-synthesis-guard.test.ts
    git add src/council/session-reset.ts src/council/session-reset-errors.ts tests/council/session-reset-synthesis-guard.test.ts
    git commit -m "feat(v0.5.2.a): SessionReset cross-table counter + synthesisInFlight guard"

---

## Task 14: CLI Adapter — `/councildone` + `/councilshow`

**Files:**
- Modify: `src/adapters/cli-commands.ts`
- Modify: `src/adapters/cli-dispatch.ts`
- Test: `tests/adapters/cli-councildone.test.ts` (new)

- [ ] **Step 1: Write failing tests**

Create `tests/adapters/cli-councildone.test.ts` covering:

- Whitelist contains both `councildone` and `councilshow`
- Unknown preset rejected
- Empty arg → `universal` preset
- `decision` arg → `decision` preset
- `/councilshow` valid id calls `fetchByThreadLocalSeq`
- `/councilshow` rejects `0`, `-1`, `3.5` (regex `^[1-9]\d{0,9}$`)
- `/councildone` reports "not configured" when `artifactService` not wired

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/adapters/cli-councildone.test.ts`. Expected: FAIL.

- [ ] **Step 3: Patch `cli-commands.ts`**

In `src/adapters/cli-commands.ts`:

1. Add to whitelist:

    export const CLI_COMMAND_NAMES: ReadonlySet<string> = new Set([
      'help', 'sessions', 'delete', 'memories', 'memory', 'forget', 'patterns',
      'councilreset', 'councilhistory',
      'councildone', 'councilshow',
      'quit', 'debug', 'resume',
    ]);

2. Add `ArtifactWiring` interface, constructor param, methods:

    import type { ArtifactService } from '../council/artifact-service.js';

    export interface ArtifactWiring {
      artifactService?: ArtifactService;
      threadId?: number;
    }

    // In CliCommandHandler class:
    private artifact: ArtifactWiring;

    constructor(
      sessions: CliSessionManager,
      db: MemoryDB,
      print: PrintFn,
      reset: ResetWiring = {},
      artifact: ArtifactWiring = {},
    ) {
      // ... existing assignments ...
      this.artifact = artifact;
    }

3. Extend `handleAsync` switch:

    async handleAsync(command: string, args: string): Promise<void> {
      switch (command) {
        case 'councilreset': return this.councilReset();
        case 'councilhistory': return this.councilHistory();
        case 'councildone': return this.councilDone(args);
        case 'councilshow': return this.councilShow(args);
      }
    }

4. Add the two methods:

    private async councilDone(args: string): Promise<void> {
      const svc = this.artifact.artifactService;
      if (!svc || this.artifact.threadId === undefined) {
        this.print('/councildone is not configured in this CLI session.');
        return;
      }
      const trimmed = args.trim();
      let preset: 'universal' | 'decision';
      if (trimmed === '' || trimmed === 'universal') preset = 'universal';
      else if (trimmed === 'decision') preset = 'decision';
      else {
        this.print('unknown preset, accepted: universal | decision');
        return;
      }
      try {
        const row = await svc.synthesize(this.artifact.threadId, preset);
        const tldrSummary = extractTldrPreview(row.content_md);
        this.print(`✅ Artifact #${row.thread_local_seq} (${row.preset}) created.`);
        this.print(`TL;DR: ${tldrSummary}`);
        this.print(`完整內容: /councilshow ${row.thread_local_seq}`);
      } catch (err) {
        this.print(`/councildone failed: ${err instanceof Error ? err.message : String(err)}`);
      }
    }

    private async councilShow(args: string): Promise<void> {
      const svc = this.artifact.artifactService;
      if (!svc || this.artifact.threadId === undefined) {
        this.print('/councilshow is not configured in this CLI session.');
        return;
      }
      const trimmed = args.trim();
      if (!/^[1-9]\d{0,9}$/.test(trimmed)) {
        this.print('/councilshow <id>，例：/councilshow 3');
        return;
      }
      const seq = parseInt(trimmed, 10);
      const row = svc.fetchByThreadLocalSeq(this.artifact.threadId, seq);
      if (!row) {
        this.print(`artifact #${seq} not found in this thread`);
        return;
      }
      this.print(row.content_md);
    }

5. Add `extractTldrPreview` helper at module level:

    function extractTldrPreview(md: string): string {
      const m = md.match(/(?:^|\n)## TL;DR\s*\n+([^\n][\s\S]*?)(?=\n## |$)/);
      if (!m) return '(missing TL;DR)';
      return m[1].trim().slice(0, 200);
    }

- [ ] **Step 4: Patch `cli-dispatch.ts` if needed**

Verify dispatch already routes via `handleAsync` for any whitelisted command (existing pattern). If there is an explicit list, add `'councildone'` and `'councilshow'` to it.

- [ ] **Step 5: Run tests, commit**

    npx vitest run tests/adapters/cli-councildone.test.ts tests/adapters/cli-flow.test.ts
    git add src/adapters/cli-commands.ts src/adapters/cli-dispatch.ts tests/adapters/cli-councildone.test.ts
    git commit -m "feat(v0.5.2.a): CLI /councildone + /councilshow wiring"

---

## Task 15: Telegram Adapter Wiring

**Files:**
- Modify: `src/adapters/telegram.ts`
- Modify: `src/telegram/bot.ts`
- Test: `tests/telegram/councildone-routing.test.ts` (new)

- [ ] **Step 1: Write guard test for threadId resolution**

Create `tests/telegram/councildone-routing.test.ts`:

    import { describe, it, expect } from 'vitest';
    import { resolveTelegramThreadId } from '../../src/telegram/handlers.js';

    describe('/councildone Telegram routing', () => {
      it('resolveTelegramThreadId reads message_thread_id (NOT chat.id)', () => {
        expect(resolveTelegramThreadId({ message_thread_id: 7 })).toBe(7);
        expect(resolveTelegramThreadId({})).toBe(0);
        expect(resolveTelegramThreadId(undefined)).toBe(0);
      });
    });

- [ ] **Step 2: Run test (should pass already)**

Run: `npx vitest run tests/telegram/councildone-routing.test.ts`. Expected: PASS — `resolveTelegramThreadId` already exists. This test guards against future regressions.

- [ ] **Step 3: Add `wireCouncilDone` / `wireCouncilShow` methods**

In `src/adapters/telegram.ts`, find existing `wireReset` method. Add parallel methods:

    private councilDoneHandler?: { synthesize: (threadId: number, preset: 'universal' | 'decision') => Promise<{ thread_local_seq: number; preset: string; content_md: string }> };
    private councilShowHandler?: { fetchByThreadLocalSeq: (threadId: number, seq: number) => { content_md: string; thread_local_seq: number } | null };

    public wireCouncilDone(handler: NonNullable<typeof this.councilDoneHandler>): void {
      this.councilDoneHandler = handler;
    }

    public wireCouncilShow(handler: NonNullable<typeof this.councilShowHandler>): void {
      this.councilShowHandler = handler;
    }

- [ ] **Step 4: Register `bot.command(...)` in `bot.ts`**

In `src/telegram/bot.ts`, BEFORE the catch-all message handler (model after the existing `bot.command('councilreset', ...)`), register:

    bot.command('councildone', async (ctx) => {
      const threadId = resolveTelegramThreadId(ctx.message);
      const text = ctx.match?.toString().trim() ?? '';
      let preset: 'universal' | 'decision';
      if (text === '' || text === 'universal') preset = 'universal';
      else if (text === 'decision') preset = 'decision';
      else {
        await ctx.reply('unknown preset, accepted: universal | decision', { message_thread_id: threadId || undefined });
        return;
      }
      if (!adapter.councilDoneHandler) {
        await ctx.reply('/councildone is not configured', { message_thread_id: threadId || undefined });
        return;
      }
      try {
        const row = await adapter.councilDoneHandler.synthesize(threadId, preset);
        const summary = extractTldrPreview(row.content_md).slice(0, 200);
        const reply = `✅ Artifact #${row.thread_local_seq} (${row.preset}) created.\n\nTL;DR: ${summary}\n\n完整內容: /councilshow ${row.thread_local_seq}`;
        await ctx.reply(reply, { message_thread_id: threadId || undefined });
      } catch (err) {
        await ctx.reply(`/councildone failed: ${err instanceof Error ? err.message : String(err)}`, { message_thread_id: threadId || undefined });
      }
    });

    bot.command('councilshow', async (ctx) => {
      const threadId = resolveTelegramThreadId(ctx.message);
      const arg = ctx.match?.toString().trim() ?? '';
      if (!/^[1-9]\d{0,9}$/.test(arg)) {
        await ctx.reply('/councilshow <id>，例：/councilshow 3', { message_thread_id: threadId || undefined });
        return;
      }
      const seq = parseInt(arg, 10);
      if (!adapter.councilShowHandler) {
        await ctx.reply('/councilshow is not configured', { message_thread_id: threadId || undefined });
        return;
      }
      const row = adapter.councilShowHandler.fetchByThreadLocalSeq(threadId, seq);
      if (!row) {
        await ctx.reply(`artifact #${seq} not found in this thread`, { message_thread_id: threadId || undefined });
        return;
      }
      const { chunkMarkdown } = await import('../util/chunk-markdown.js');
      const chunks = chunkMarkdown(row.content_md, 4096);
      for (const chunk of chunks) {
        await ctx.reply(chunk, { message_thread_id: threadId || undefined });
      }
    });

(`adapter` here is the same outer-scope adapter reference the existing `bot.command('councilreset', ...)` handler accesses — copy the access pattern verbatim.)

- [ ] **Step 5: Run tests, type-check, commit**

    npx vitest run tests/telegram/
    npx tsc --noEmit
    git add src/adapters/telegram.ts src/telegram/bot.ts tests/telegram/councildone-routing.test.ts
    git commit -m "feat(v0.5.2.a): Telegram /councildone + /councilshow wiring"

---

## Task 16: Wire Everything Together in `src/index.ts`

**Files:**
- Modify: `src/index.ts`

- [ ] **Step 1: Patch `index.ts`**

1. Imports:

    import { effectiveRoleType, type AgentConfig } from './types.js';
    import { ArtifactDB } from './council/artifact-db.js';
    import { ArtifactService } from './council/artifact-service.js';

2. Add `pickFirstPeerConfig` near top of file:

    function pickFirstPeerConfig(agents: AgentConfig[]): AgentConfig {
      return agents.find(a => effectiveRoleType(a) === 'peer') ?? agents[0];
    }

3. Update line ~130 (`listenerAgent`) and ~154 (`mainProvider`):

    const listenerAgent = councilConfig.participation?.listenerAgent || pickFirstPeerConfig(agentConfigs).id;
    const mainProvider = getOrCreateProvider(pickFirstPeerConfig(agentConfigs).provider);

4. Update `peerWorkers` filter to use `effectiveRoleType`:

    const peerWorkers = workers.filter(w => effectiveRoleType(w.config) === 'peer');

(Find actual field path — adapt `w.config` if necessary.)

5. Construct `ArtifactDB` + `ArtifactService` near other DB inits:

    const artifactDB = new ArtifactDB(resolve('data/council.db'));

    const synthesizerConfig = agentConfigs.find(a => effectiveRoleType(a) === 'artifact-synthesizer') ?? null;

    const artifactService = new ArtifactService({
      synthesizerConfig,
      artifactDb: artifactDB,
      resetDb: resetSnapshotDB,
      handler: deliberationHandler,
      bus,
    });

6. Update `SessionReset` constructor call:

    const sessionReset = new SessionReset(resetSnapshotDB, artifactDB, facilitator);

7. Wire CLI:

    const cliHandler = new CliCommandHandler(
      cliSessions, memoryDb, printFn,
      { sessionReset, deliberationHandler, resetSnapshotDB, threadId: 0 },
      { artifactService, threadId: 0 },
    );

8. Wire Telegram adapter:

    adapter.wireCouncilDone(artifactService);
    adapter.wireCouncilShow(artifactService);

- [ ] **Step 2: Type-check, run full suite, commit**

    npx tsc --noEmit
    npx vitest run
    git add src/index.ts
    git commit -m "feat(v0.5.2.a): wire ArtifactService into index.ts (CLI + Telegram)"

---

## Task 17: Integration Tests

**Files:**
- Create: `tests/integration/council-done-flow.test.ts`
- Create: `tests/integration/council-done-guards.test.ts`

- [ ] **Step 1: Read `tests/integration/reset-flow.test.ts` for the integration setup pattern**

Mirror its real-handler + real-DBs + mock-provider setup.

- [ ] **Step 2: Write end-to-end happy-path tests**

Create `tests/integration/council-done-flow.test.ts` covering:

- Full happy path: push 3 messages → `synthesize(1, 'universal')` → row exists in DB, segment count grew by 1, `artifact.created` event fired, sealCurrentSegment called with `null`, openNewSegment called.
- Spec invariant 5b (cache invalidation on reset between done calls): seal segment 0 with `/councildone` → push 1 message in segment 1 → `/councilreset` (seals segment 1 in reset_snapshots) → push 1 message in segment 2 → `/councildone` → produces segment_index = 2 NOT cached segment-0 row.
- Spec invariant 5 (idempotent): push 2 msgs → `/councildone` → cached row returned on second `/councildone` with empty open segment, provider call count is exactly 1 across both invocations.

- [ ] **Step 3: Write guard integration tests**

Create `tests/integration/council-done-guards.test.ts` covering:

- `/councildone` blocks `/councilreset`: mock provider hangs on chat. Start `synthesize()` in background; concurrently call `sessionReset.reset()` → throws `SynthesisInProgressError`.
- `/councildone` refuses while `resetInFlight` is true.
- `/councilshow` returns artifact for matching thread, `null` for other thread (cross-thread leak prevention).
- Missing config: `synthesizerConfig === null` → `/councildone` throws `MissingSynthesizerConfigError`, no startup crash.

- [ ] **Step 4: Run all integration tests, commit**

    npx vitest run tests/integration/council-done-flow.test.ts tests/integration/council-done-guards.test.ts
    npx vitest run
    git add tests/integration/council-done-flow.test.ts tests/integration/council-done-guards.test.ts
    git commit -m "test(v0.5.2.a): integration tests for /councildone happy path + guards"

---

## Task 18: Migration Test (DB Scoping)

**Files:**
- Create: `tests/storage/artifact-migration.test.ts`

- [ ] **Step 1: Write migration test**

Create `tests/storage/artifact-migration.test.ts` covering:

- `council.db` migration: pre-populate v0.5.1 council.db with a reset snapshot. Run `new ArtifactDB(councilPath)`. Assert reset snapshot row count unchanged + `council_artifacts` table exists with 0 rows.
- `brain.db` non-impact: pre-populate brain.db with a memory row. Run artifact migration on a DIFFERENT path (council.db). Re-open brain.db raw via `better-sqlite3`, query `sqlite_master`, assert `council_artifacts` does NOT appear in brain.db.

- [ ] **Step 2: Run, commit**

    npx vitest run tests/storage/artifact-migration.test.ts
    git add tests/storage/artifact-migration.test.ts
    git commit -m "test(v0.5.2.a): migration test scopes by council.db / brain.db"

---

## Task 19: Documentation + CHANGELOG + Version Bump

**Files:**
- Create: `docs/synthesizer-config.md`
- Modify: `CHANGELOG.md`
- Modify: `package.json`

- [ ] **Step 1: Write `docs/synthesizer-config.md`**

    # `artifact-synthesizer` Worker Config

    `/councildone` requires a worker config with `role_type: artifact-synthesizer` to produce artifacts. Without one, `/councildone` rejects with a friendly "not configured" message.

    ## Adding a synthesizer

    Create a YAML file under `config/agents/`:

    ```yaml
    id: synth
    name: Artifact Synthesizer
    provider: claude         # or openai / google / custom
    model: claude-sonnet-4-6
    memory_dir: data/synthesizer-memory
    personality: ""          # IGNORED by /councildone — synthesizer goes through provider.chat directly
    role_type: artifact-synthesizer
    ```

    Restart the bot. On startup the synthesizer config is detected and `ArtifactService` becomes available.

    ## Notes

    - The synthesizer worker is **excluded** from the peer pool — it never participates in normal `/council` deliberations.
    - The synthesizer's provider is **lazy-instantiated** — only constructed when `/councildone` actually runs, so adding a config that references a not-yet-set env var doesn't break startup.
    - `personality.ts`'s markdown ban is bypassed for `/councildone` (synthesizer needs to emit markdown headings).

- [ ] **Step 2: Append CHANGELOG entry**

    ## v0.5.2.a — 2026-04-26

    ### Added

    - `/councildone [universal|decision]` user command synthesizes a TL;DR-bearing markdown artifact from the current segment's discussion, persists it, and opens a new segment. Idempotent: a second `/councildone` on an empty new segment returns the cached artifact (zero LLM calls).
    - `/councilshow <id>` retrieves a previously created artifact's full content (chunked at 4096 chars on Telegram).
    - New `artifact-synthesizer` worker `role_type` (excluded from the peer pool, never invoked during normal deliberation). See `docs/synthesizer-config.md`.
    - Bidirectional `synthesisInFlight` lock: `/councildone` and `/councilreset` mutually exclude one another; `runDeliberation` refuses while synthesis is in flight.
    - New `council_artifacts` table in `data/council.db` (additive migration).
    - New `EventMap['artifact.created']` event (broadcast only).

    ### Changed

    - `AgentConfig.roleType` is now optional `WorkerRoleType | undefined`. Use `effectiveRoleType()` to resolve `undefined` → `'peer'` at use sites.
    - `src/index.ts` startup provider + listener fallback prefer the first peer config.
    - `SessionReset.reset` segment-index calculation now consults BOTH `session_reset_snapshots` and `council_artifacts` via the shared `computeNextSegmentIndex` helper.
    - `CustomProvider` thrown errors now carry `.status` so `isHardFail` can distinguish 4xx (hard) vs 429/5xx (retry).

    ### Notes

    - Concurrent retry storm under provider hang is a known limitation; spec §5 documents it. v0.5.3+ AbortSignal threading is the proper fix.

- [ ] **Step 3: Bump version**

In `package.json`, change `"version": "0.5.1"` → `"version": "0.5.2"`.

- [ ] **Step 4: Commit**

    git add docs/synthesizer-config.md CHANGELOG.md package.json
    git commit -m "docs(v0.5.2.a): synthesizer config doc, CHANGELOG, version bump"

---

## Task 20: Local CI Parity + PR

- [ ] **Step 1: Run full local CI parity** per `feedback_local_ci_parity_before_push.md`:

    cd ~/Projects/agent-council
    npm ci
    npx tsc --noEmit
    npm test
    npm run build

All four must be green. If any fail, FIX before pushing.

- [ ] **Step 2: Push branch**

    git push -u origin v0.5.2.a-councildone-artifact

- [ ] **Step 3: Create PR**

Use `gh pr create` with a body describing summary + test plan. Include note: "13 codex review rounds, 0 P1 ship gate". Link the spec and plan files.

- [ ] **Step 4: After PR opens — run cross-model review** per `feedback_cross_model_review_catches_claude_blindspots.md`:

    codex review --base main --title "v0.5.2.a impl review"

Capture output, address any P1 findings before requesting human review. P3 from spec round-13 (ProviderTimeoutError specific test in `artifact-invoke.test.ts`) — verify it's covered in Task 9 tests; if not, add.

---

## Self-Review Checklist

**Spec coverage:**

- §0 scope split → respected (no contract layer)
- §1-2 problem/goal → Tasks 11-15
- §3 architecture flow → Tasks 11-12 + integration in 17
- §4 data model + helpers → Task 6 (ArtifactDB) + Task 7 (computeNextSegmentIndex) + Task 11 (lastSealedSegmentIndex)
- §5 worker model + invocation API → Tasks 1, 8, 9, 4 (CustomProvider .status)
- §6 command routing + threadId resolution → Tasks 14 (CLI), 15 (Telegram + threadId guard test)
- §7 concurrency + bidirectional lock → Tasks 10, 13
- §8 synthesis pipeline (phase 1/2/3) → Tasks 11, 12
- §9 user surface + chunking → Tasks 3, 14, 15
- §10 missing-config behavior → Task 11 (Phase 1 first check) + Task 19 (synthesizer-config.md)
- §11 invariants → distributed across unit + integration tests; 5b reset cache test in Task 17, 7a malformed in Task 12
- §12 files touched → matched by file structure + Tasks 1-16 (with documented deviation: CLI/Telegram commands stay in adapter pattern, NOT new `src/cli/commands/` or `src/telegram/commands/` directories)
- §13 migration → Task 18 (DB scoping)
- §19.1 cross-module helper → Task 7 (Option A: shared module)
- §19.2 effectiveRoleType placement → Task 1 (in src/types.ts)
- §19.3 follow-ups → covered (Task 0 .gitignore, Task 9 tests cover ProviderTimeoutError)

**Type consistency:**

- `WorkerRoleType` (Task 1) used in Tasks 16 — consistent
- `ArtifactRow` (Task 6) used in Tasks 11-12 — consistent
- `ArtifactService` ctor `{ synthesizerConfig, artifactDb, resetDb, handler, bus }` (Task 11) used by Task 16 — consistent
- `HandlerForArtifact` `setSynthesisInFlight` / `isSynthesisInFlight` (Task 11) match Task 10's `DeliberationHandler` methods — consistent
- `computeNextSegmentIndex(threadId, resetDb, artifactDb, handler)` (Task 7) used by Task 12 (ArtifactService) and Task 13 (SessionReset) — consistent
- `chunkMarkdown(text, maxChars)` (Task 3) used by Task 15 — consistent
- `parseArtifact(content): { tldr: string | null }` (Task 8) used by Task 12 — consistent

**Placeholders:** none. Tests in Tasks 10 and 13 reference existing test factory patterns by name (`tests/council/deliberation-segments.test.ts`, `tests/council/session-reset.test.ts`) so the engineer copies real working code, not placeholder pseudocode.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-04-26-v0.5.2.a-councildone-artifact.md`. Two execution options:

1. **Subagent-Driven (recommended)** — fresh subagent per task with two-stage review (TDD discipline + spec compliance). Best fit for this plan's bounded-task structure. Uses `superpowers:subagent-driven-development`.

2. **Inline Execution** — execute tasks in this session, batch with checkpoints. Uses `superpowers:executing-plans`. Reasonable for fast solo iteration but loses cross-model verification benefit.

**Which approach?**
